import declaraciones from '../data/declaraciones.json';
import { cambio } from '../lib/altoImpacto';
export default function Declaraciones(){return <div className="declaraciones"><h4>Expediente de afirmaciones públicas</h4><p>Una ficha documentada no equivale a una afirmación verificada. Se distingue la comprobación aritmética de la validación de las fuentes.</p>{declaraciones.map(d=><article className="tarjeta impacto-panel" key={d.id}><p className="impacto-eyebrow">{d.fecha} · {d.tipoFuente}</p><h4>{d.estado}</h4><p>{d.resumen}</p><dl><dt>Valores transcritos de la cobertura, pendientes de confirmación primaria</dt><dd>{d.baseReportada} → {d.actualReportado}</dd><dt>Operación con esos dos números</dt><dd>{cambio(d.actualReportado,d.baseReportada)?.toFixed(2)} %; redondeo entero: {Math.round(cambio(d.actualReportado,d.baseReportada)!)} %</dd></dl><p>{d.limite}</p><p className="nota">La consistencia del redondeo no certifica la unidad, el origen de las cifras, la tendencia ni una atribución causal. No se compara esta ficha con la serie de delitos del sitio.</p><a href={d.fuente}>Consultar cobertura periodística</a></article>)}</div>;}
